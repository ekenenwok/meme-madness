export const metadata = {
  title: "Meme Madness",
  description: "Pick your trader. Catch the pumps. Dodge the rug pulls.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0 }}>{children}</body>
    </html>
  );
}
